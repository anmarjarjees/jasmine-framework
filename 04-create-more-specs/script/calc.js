// we will modify this function but after running the test and see the error:
// adding two arguments and do the right calculations  
function addition(numOne, numTwo) {
    /*
That helps us to ensure that no matter what values we pass through, we're going to get the sum of those two numbers as opposed to just being given a hard-coded response of 42 as it was before!
    */
    return numOne + numTwo;
}

// Test will pass now after modifying this function